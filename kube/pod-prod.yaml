apiVersion: v1
kind: Pod
metadata:
  name: cuda-nvidia-smi
spec:
  restartPolicy: Never
  containers:
    - name: cuda
      image: docker.io/nvidia/cuda:12.4.1-base-ubuntu22.04
      command: ["nvidia-smi"]
      resources:
        limits:
          "nvidia.com/gpu=all": 1