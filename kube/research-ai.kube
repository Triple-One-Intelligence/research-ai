[Unit]
Description=Research AI Production Service
After=network-online.target

[Kube]
Yaml=/etc/research-ai/bundle.yaml
PublishPort=80:80
PublishPort=443:443
PodmanArgs=--device nvidia.com/gpu=all

[Install]
WantedBy=default.target
