[pytest]
testpaths = tests
asyncio_mode = auto
addopts =
    -v
    --tb=short
    --no-header
    -q
markers =
    unit: Unit tests (no services needed)
    smoke: Smoke tests (need running dev/prod environment)
    integration: Integration tests (need running dev environment)
